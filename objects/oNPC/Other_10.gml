/// @description Idle State

image_speed = 0.3;


/*
if (alarm[0] <= 0 && !isCalling) {
	alarm[0] = random_range(2, 4) * global.oneSecond;
	currentState = NPC.MOVING;
	_direction = random(360);
}

*/
